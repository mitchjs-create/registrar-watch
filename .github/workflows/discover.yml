name: Discover booking flows

on:
  workflow_dispatch:
    inputs:
      site:
        description: "Site key to inspect, or 'all'"
        required: true
        default: "all"
      mode:
        description: "explore (walk the flow) or capture (first screen only)"
        required: true
        default: "explore"

permissions:
  contents: write

jobs:
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run discovery
        run: |
          SITE_ARG=""
          if [[ "${{ inputs.site }}" != "all" ]]; then SITE_ARG="--site=${{ inputs.site }}"; fi
          if [[ "${{ inputs.mode }}" == "explore" ]]; then
            node explore.js $SITE_ARG 2>&1 | tee debug-summary.txt
          else
            node check.js --dry-run $SITE_ARG 2>&1 | tee debug-summary.txt
          fi
        continue-on-error: true

      - name: Commit findings
        run: |
          mkdir -p debug
          mv -f debug-summary.txt debug/ 2>/dev/null || true
          git config user.name "registrar-watch"
          git config user.email "actions@users.noreply.github.com"
          git add -A debug state.json 2>/dev/null || true
          if [[ -n "$(git status --porcelain)" ]]; then
            git commit -m "Discovery run: ${{ inputs.site }}"
            git pull --rebase --autostash
            git push
          else
            echo "Nothing captured"
          fi
